import styled from "styled-components";

export const FooterContainer = styled.div`
  display: flex;
  gap: 20px;
  position: absolute;
  bottom: 10px;
  right: 10px;
  img {
    cursor: pointer;
  }
`